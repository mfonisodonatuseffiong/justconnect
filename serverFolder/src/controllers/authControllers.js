/**
 * @desc in this file is where we have all the routes http functions
 *        - Only for all auth routes like login, logout, register, forgetpassword , reset password and checkme
 */


export const checkMeController = async () => {
    // this uses the middleware to check if token exist in the cookies and user is logged in 
}


export const loginController = async () => {
    // run a try catch a
}

export const logoutController = async () => {
    // your forget password logic
}

export const registerController = async () => {
    // your register logic
}

export const forgetPasswordController = async () => {
    // your forget password logic
}

export const resetPasswordController = async () => {
    // reset password
}

V1 shows this is the version 1 of apis and if there is any modification there can be a V2 where the new modified endpoit can be created without breaking V1 routes

This is the route file for all routes endpoint for
- authentication
- other project routing

example a user tries to send a post method to the backend to login
URL - POST localhost:5000/api/v1/auth/login


Starts The server.js file - api/v1
Next: Moves into the V1 folder and then hits index.js which serach for - /auth
Next: Moves into the auth routes and look for the final end point - /login 
